module.exports = {
  // plugin name, must be same as node module name
  pluginName: 'pug-plugin',
  // label in terminal output from plugin
  pluginLabel: 'Pug Plugin',
  // label in terminal output from loader
  loaderLabel: 'Pug Loader',
};
