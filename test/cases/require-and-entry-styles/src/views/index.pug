html
  head
    //- the SCSS file from source
    link(rel='stylesheet' href=require('~Styles/main.scss'))
    //- the CSS file from source
    link(rel='stylesheet' href=require('~Styles/about.css'))
    //- the compiled CSS from public path
    link(rel='stylesheet' href='/assets/css/style.css')
  body
    p The usage of the style source and the compiled styles from webpack entry.