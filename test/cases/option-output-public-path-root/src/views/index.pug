html
  head
    //- test for auto publicPath when used same asset from different paths
    link(href=require('Styles/common.scss') rel='stylesheet')
    link(href=require('./style.scss') rel='stylesheet')
    //- test to resolve script filename w/o extension `.js`
    script(src=require('Scripts/common.min'))
    script(src=require('./main.js'))
  body
    .header Header
    h1 Home
    img(src=require('Images/image.png'))