- var DATA = require('../data/home.json');

doctype html
html

  include ./includes/head.pug
  include ./includes/menuItem.pug

  head
    +head(DATA)

  body
    +menuItem('home')
    h1 Home
    img(src=require(DATA.body.image))